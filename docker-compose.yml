version: '3.8'

services:
  rsspub:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./static/epubs:/app/static/epubs
    environment:
      - RUST_LOG=info
    restart: unless-stopped
